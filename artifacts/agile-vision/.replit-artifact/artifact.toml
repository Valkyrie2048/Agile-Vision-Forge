kind = "web"
previewPath = "/"
title = "Agile Vision"
version = "1.0.0"
id = "artifacts/agile-vision"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21879

[services.development]
run = "pnpm --filter @workspace/agile-vision run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/agile-vision", "run", "build" ]
publicDir = "artifacts/agile-vision/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21879"
BASE_PATH = "/"
